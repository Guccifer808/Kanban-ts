//Columns
export enum ColumnType {
    TO_DO = 'Todo',
    IN_PROGRESS = 'In Progress',
    COMPLETED = 'Completed',
    BLOCKED = 'Blocked',
}
//Drag n drop
export enum ItemType {
    TASK = 'Task',
  }